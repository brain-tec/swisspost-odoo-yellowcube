<?xml version="1.0" encoding="utf-8"?>
<openerp>

    <!-- We define here the data should not be modified by the user. -->
	<data noupdate="0">
		<record id="cron_main_tag_from_parent" model="ir.cron">
			<field name="name">PCAP - Copy Main Tag from Parent</field>
			<field name="model">res.partner</field>
			<field name="function">cron_main_tag_from_parent</field>
            <field name="args" eval="()" />
		</record>
    </data>

    <!-- We define here the data which may be modified by the user. -->
	<data noupdate="1">
		<record id="cron_main_tag_from_parent" model="ir.cron">
			<field name="interval_type">weeks</field>
			<field name="interval_number" eval="1" />
			<field name="numbercall" eval="-1" />
			<field name="doall" eval="True" />
			<field name="priority" eval="0" />
			<field name="active" eval="False" />
		</record>
    </data>

</openerp>
